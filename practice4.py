# The Scenario: You need a quick router function that handles the status of an item in your system.

# Your Task:
# Write a standard function (outside of a class) called route_workflow(status).

# Use Python 3.10 match and case statements.

# If the status is "Submitted", return "Routing to Manager".

# If the status is "Approved", return "Updating Database".

# If the status is "Rejected", return "Notifying Employee".

# Add a default case that returns "Invalid Status".







explain me decorators ? why it is used ? where it is used ? how it is used ? and what happen if we dont use it ? types of decorators ?



class a:
    def route_workflow(self):
        return "Routing to Manager"

class b:
    def route_workflow(self):
        return "Updating Database"     

class c:
    def route_workflow(self):
        return "Notifying Employee"           

def route_workflow(status):
    if Status == "submitted"
        print()

    if status == "rejected":
        print(status.)    